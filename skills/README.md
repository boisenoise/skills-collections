# Skills Directory

This directory will be populated with skills after running the setup script.

Run `./scripts/setup.sh` to fetch all skills from configured sources.

See `CATALOG.md` (generated after setup) for the full list of available skills.
